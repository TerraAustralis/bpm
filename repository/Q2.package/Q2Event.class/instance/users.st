evaluating
users
	"Some events know the (small) subset of users able to subscribe for the event for a specific 
	primary object. These events should override this method and return this subset of users.
	E.g. model administrators, or all users."

	^nil