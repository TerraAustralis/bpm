as yet unclassified
append: anEntry
  "Append data from anEntry to receiver.
	Shall we check, if we adding data entries for same date(s)? And ignore or replace them?"

  statistic addAll: anEntry data