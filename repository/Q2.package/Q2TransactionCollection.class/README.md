I am used to hold a local collection of txn's within a case. I cache #firstAgeInSeconds and #lastAgeInSeconds and also #size is available without loading the objects in the array.

For more detailed queries, there is a global txn log which is a MagmaCollection.