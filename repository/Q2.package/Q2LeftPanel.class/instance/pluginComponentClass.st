plugins
pluginComponentClass
"return the class, which will be used for encapsulation of plugins which will be rendered at receiver spot,
returning nil means that component doesn't have a spots where plugins can be placed"
	^ Q2LeftPanelPluginSpot